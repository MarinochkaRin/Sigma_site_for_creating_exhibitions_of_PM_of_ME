package com.mryndina.exhibitions.service.impl;

/*import com.mryndina.exhibitions.service.FileService;
import com.mryndina.exhibitions.util.files.File;
import com.mryndina.exhibitions.util.files.S3Service;
import lombok.AllArgsConstructor;
import org.springframework.stereotype.Service;
import org.springframework.web.multipart.MultipartFile;*/

/*@Service
@AllArgsConstructor
public class FileServiceImpl implements FileService {

    private final S3Service imageFileClient;

    @Override
    public File resolveFile(String filename, String path) {
        return imageFileClient.getFile(filename, path);
    }

    @Override
    public void uploadFile(String filename, String path, MultipartFile file) {
        imageFileClient.putFile(filename, path, file);
    }

}*/
